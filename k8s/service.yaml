apiVersion: v1
kind: Service
metadata:
  name: pdf-parser
  labels:
    app.kubernetes.io/name: pdf-parser
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: pdf-parser
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
